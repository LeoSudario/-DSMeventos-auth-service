# ===========================================
# DSMeventos Auth Service - Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA faça commit do arquivo .env no controle de versão!

# Configuração do Banco de Dados
# Formato: mongodb+srv://usuario:senha@host/nomedatabase?opcoes
DATABASE_URL="mongodb+srv://usuario:senha@cluster.mongodb.net/nomedatabase"

# Configuração do Servidor
# Porta - O Render configura automaticamente em produção
PORT=3001

# Configuração JWT
# Gere uma string aleatória segura (mínimo 32 caracteres)
# Você pode gerar usando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="sua-chave-secreta-jwt-minimo-32-caracteres"